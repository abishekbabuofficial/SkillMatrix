User Services 
------------------
getUserById(id)

getAllUsers(filter?)

getUsersByTeam(teamName)

getSkillMatrixByTeam(teamName) //skills

getFullMatrix() // for HR

HR services ---------
createUser(data)

updateUser(id, data)

deleteUser(id)

Skill Services
----------------------
createSkill(data)

getAllSkills()

getSkillsByPosition(position)

getSkillById(id)

deleteSkill(id)

Skill Update Request Services
-------------------------------------
createRequest(userId, skillScore)

getRequestById(id)

getRequestsByUser(userId)

getPendingRequestsByLead(leadId)

updateRequestStatus(id, status, reviewerId, editedScore)

cancelRequest(id)


Skill Upgrade Guide
-------------------------
createGuide(skillId, fromLevel, toLevel, guidance, resourceLink)

getGuide(skillId, fromLevel, toLevel)

getAllGuidesBySkill(skillId)

deleteGuide(id)